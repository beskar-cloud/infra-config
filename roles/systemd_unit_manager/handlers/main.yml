---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes
  become: true

- name: Start systemd unit
  ansible.builtin.systemd:
    name: "{{ item.name }}"
    state: "started"
  loop: "{{ systemd_units }}"
  when: item.state == 'started'
  become: true
